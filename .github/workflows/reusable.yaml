name: Reusable
on:
  workflow_call:

jobs:
  witness:
    name: Test Output
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        run: |
          while read line; do echo $line; done < test.txt
